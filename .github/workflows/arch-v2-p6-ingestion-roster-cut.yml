name: ARCH V2 P6 Ingestion Roster Cut

on:
  push:
    branches:
      - architecture-v2
    paths:
      - '.github/workflows/arch-v2-p6-ingestion-roster-cut.yml'

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'P6 workflow trigger is active'
