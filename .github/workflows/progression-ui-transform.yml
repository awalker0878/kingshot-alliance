name: Progression UI Transform

on:
  push:
    branches:
      - progression-capability-completion
    paths:
      - scripts/progression/apply_completion_ui.py
      - .github/workflows/progression-ui-transform.yml

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.2.2
        with:
          ref: progression-capability-completion
          fetch-depth: 0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'
      - run: python scripts/progression/apply_completion_ui.py
      - name: Commit transformed UI
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add resources/js/components/progression resources/js/localization/messages/progression/en.ts
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'feat: add structured Progression screenshot review UI'
          git push origin HEAD:progression-capability-completion
