import type { MessageCatalogue } from '../../types';

const messages = {
  platformAdmin: {
    title: 'Administrasi platform',
    localizationRuntime: 'Runtime lokalisasi',
  },
} satisfies MessageCatalogue;

export default messages;
