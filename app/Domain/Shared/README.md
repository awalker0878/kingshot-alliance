# Shared domain foundation

This directory is reserved for small, stable concepts genuinely shared by implemented domains. It must not become a miscellaneous helper directory or contain speculative product models.
