# Infrastructure layer

Infrastructure adapters implement persistence, queues, storage, telemetry, and external integrations. Domain and application code must not depend directly on provider-specific clients.
